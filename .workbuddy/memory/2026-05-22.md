# 2026-05-22 工作日志

## 完成：A股15只赚钱天团监测系统 — 频率调整 + 标题更名

### 背景
用户要求将原"A股巨无霸高股息监测系统"更名为" A股15只赚钱天团股票监测面板"，
并将 GitHub Actions 自动更新频率从"每小时"改为"每天10:00和14:00（北京时间）"。

### 完成的改动

#### 1. 更新频率调整（4个文件）
- `.github/workflows/update_data.yml`：cron 从 `0 1-7 * * 1-5`（每小时）改为 `0 2,6 * * 1-5`（每天两档）
  - 北京 10:00 = UTC 02:00
  - 北京 14:00 = UTC 06:00
  - 仅周一至周五运行
- `README.md`：功能表和FAQ表更新频率说明
- `stock_monitor.py`：文件头注释更新
- `index.html`：页脚"更新频率"文字更新

#### 2. 标题全面更名（5处）
- `index.html` 第7行：`<title>` 标签
- `index.html` 第289行：`<h1>` 页面标题
- `README.md` 第1行：`# ` 大标题
- `README.md` 第3行：`> 一句话介绍` 描述语
- `stock_monitor.py` 第4-5行：文件头注释

### 技术细节
- GitHub Actions cron 语法：`0 2,6 * * 1-5` = 每周一至周五的 UTC 02:00 和 06:00
- 时区对照：UTC+8 = 北京时间，所以 UTC 02:00 = 北京 10:00，UTC 06:00 = 北京 14:00
- 选择这两个时间点的原因：10:00 是开盘后1小时数据已稳定，14:00 是尾盘前1小时适合尾盘决策

### 项目文件清单（最终版）
1. `stock_monitor.py` - Python数据抓取脚本
2. `index.html` - 可视化面板（深色主题，响应式）
3. `data.json` - 数据文件（脚本自动生成，已附模拟数据）
4. `requirements.txt` - Python依赖
5. `.github/workflows/update_data.yml` - GitHub Actions配置（每天10:00/14:00运行）
6. `README.md` - 大白话0基础5步部署教程

### 待办
- 用户尚未提供 GitHub 仓库地址，无法帮忙 push
- 用户如需增加/删除监测股票，需同步修改 `stock_monitor.py` 的 STOCK_LIST 和 CATEGORY_GROUPS

---

## 完成：A股15只赚钱天团监测系统 — 前瞻股息率数据源切换

### 背景
用户要求：
1. 通过 akshare/baostock 查询2025年分红数据（包含已确认分红和分红预案）
2. 整理成 `FORWARD_DIVIDEND.json` 文件
3. 更新 HTML 面板从 `FORWARD_DIVIDEND.json` 文件读取前瞻股息率

### 完成的改动

#### 1. 创建 `FORWARD_DIVIDEND.json` 文件
- 使用 akshare 的 `ak.stock_dividend_cninfo()` 接口获取15只股票的2025年分红数据
- 数据格式：
  ```json
  {
    "601166": {
      "name": "兴业银行",
      "dividends": [
        {"报告时间": "2025半年报", "分红说明": "10派5.65元(含税)", "每股分红": 0.565},
        {"报告时间": "2025年报预案", "分红说明": "10派5.01元(含税)", "每股分红": 0.501}
      ],
      "total_dividend_per_share": 1.066,
      "forward_dividend_yield_base": 1.066
    }
  }
  ```

#### 2. 更新 `stock_monitor.py`
- 移除硬编码的 `FORWARD_DIVIDEND` 字典
- 新增 `load_forward_dividend()` 函数，从 `FORWARD_DIVIDEND.json` 读取分红数据
- 更新 `build_stock_data()` 函数，从 `FORWARD_DIVIDEND_DATA` 读取 `total_dividend_per_share`
- 计算前瞻股息率：`(total_dividend_per_share / 当前股价) × 100%`

#### 3. 更新 `index.html`
- 将所有 `dividend_yield_real` 字段替换为 `dividend_yield`（与 `data.json` 输出一致）
- 确保正确显示前瞻股息率

#### 4. 解决 Git 合并冲突
- 远程仓库有新的提交（`4d35890`）
- 使用 `git fetch` + `git reset --soft` 策略，避免 rebase 编辑器问题
- 成功解决 `data.json` 的合并冲突（使用本地版本）

#### 5. 提交代码到 GitHub
- Commit hash: `b07cf8a`
- 提交消息：`feat: 从 FORWARD_DIVIDEND.json 读取前瞻股息率`
- 已推送到 `https://github.com/moregun/stock-monitor-workbuddy.git`

### 技术细节
- akshare 接口：`ak.stock_dividend_cninfo(symbol=code)` 可获取 A股分红数据
- 数据格式：返回 DataFrame，包含 `实施方案分红说明`、`报告时间` 等字段
- 正则提取：`r'10派(\d+\.?\d*)'` 从分红说明中提取每股分红
- 前瞻股息率计算：`(每股分红总和 / 当前股价) × 100%`
- JSON 文件读取：`json.load(open('FORWARD_DIVIDEND.json', 'r', encoding='utf-8'))`

### 项目文件清单（更新版）
1. `stock_monitor.py` - Python数据抓取脚本（从 JSON 读取分红数据）
2. `index.html` - 可视化面板（深色主题，响应式）
3. `data.json` - 数据文件（脚本自动生成）
4. `FORWARD_DIVIDEND.json` - 2025年分红数据文件（新增）
5. `requirements.txt` - Python依赖
6. `.github/workflows/update_data.yml` - GitHub Actions配置
7. `README.md` - 部署教程

### 测试结果
- ✅ `stock_monitor.py` 成功运行，生成 `data.json`
- ✅ 兴业银行前瞻股息率：6.11%（正确）
- ✅ 信号判断：绿色（可买入）
- ✅ `index.html` 正确显示前瞻股息率

### 待办
- 用户需手动触发 GitHub Actions 工作流 "更新股票监测数据"
- 验证 `data.json` 是否正确更新
- 如成功，启用 GitHub Pages（如尚未启用）
